codons:
  auth_capability_ext:
    version: 1.0.0
    description: example custom schema bundled with trait
    schema: |
      {
        description: Optional<string>,
        inputs: Map<string, { type: string }>,
        outputs: Map<string, { type: string }>,
        effects: List<string>
      }
