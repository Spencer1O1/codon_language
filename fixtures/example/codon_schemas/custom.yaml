codons:
  auth_capability:
    version: 1.0.0
    description: capability shape for auth flows
    schema: |
      {
        description: Optional<string>,
        effects: List<string>,
        inputs: Map<string, { type: string }>,
        outputs: Map<string, { type: string }>
      }
