gene: classifier
description: AI classification of damage photos
codons:
  entities:
    classification:
      id: uuid
      photo_id: uuid          # tickets.photo
      label: string
      confidence: number
  capabilities:
    classify_damage:
      description: AI model classifies uploaded photo.
      effects: ["classify_photo"]
      inputs:
        photo_id: { type: uuid }
      outputs:
        classification_id: { type: uuid }
  relations:
    photo_classifications:
      from: tickets.tickets.photo
      to: classification
      type: one-to-many
      ownership: from
      cascade: restrict
  traits: []
